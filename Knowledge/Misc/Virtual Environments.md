```bash
# Create a virtual environmment called "environment"
python -m venv environment

# Activate it
source environment/bin/activate

# You can now install new packages freely :)
pip3 install wsgidav
```
